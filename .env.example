# Usato solo per lo sviluppo locale (node build/index.js), non serve quando il connettore
# è installato come estensione .mcpb in Claude Desktop: in quel caso il token viene
# richiesto tramite le impostazioni dell'estensione.
FIC_ACCESS_TOKEN=
FIC_COMPANY_ID=
